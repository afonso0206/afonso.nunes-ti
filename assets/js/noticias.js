(() => {
 const fmt=v=>{const[y,m,d]=v.split('-').map(Number);return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'long',year:'numeric'}).format(new Date(y,m-1,d))};
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
 const media=x=>x?.imagem?.src?`<figure class="news-cover"><img src="${esc(x.imagem.src)}" alt="${esc(x.imagem.alt||x.titulo)}" loading="lazy">${x.imagem.legenda?`<figcaption>${esc(x.imagem.legenda)}</figcaption>`:''}</figure>`:'';
 const blocks=x=>{if(Array.isArray(x.blocos)&&x.blocos.length)return x.blocos.map(b=>b.tipo==='imagem'?`<figure class="news-inline-image"><img src="${esc(b.src)}" alt="${esc(b.alt||'')}" loading="lazy">${b.legenda?`<figcaption>${esc(b.legenda)}</figcaption>`:''}</figure>`:`<p>${esc(b.texto||'')}</p>`).join('');return x.conteudo?`<p>${esc(x.conteudo)}</p>`:''};
 const url=x=>`${location.origin}${location.pathname}#${encodeURIComponent(x.id)}`;
 const share=x=>{const u=encodeURIComponent(url(x)),t=encodeURIComponent(x.titulo);return `<div class="news-share" aria-label="Compartilhar atualização"><span>Compartilhar:</span><a target="_blank" rel="noopener" href="https://wa.me/?text=${t}%20${u}" aria-label="Compartilhar no WhatsApp">WhatsApp</a><a target="_blank" rel="noopener" href="https://www.linkedin.com/sharing/share-offsite/?url=${u}" aria-label="Compartilhar no LinkedIn">LinkedIn</a><a target="_blank" rel="noopener" href="https://www.facebook.com/sharer/sharer.php?u=${u}" aria-label="Compartilhar no Facebook">Facebook</a><button type="button" class="copy-news-link" data-url="${esc(url(x))}">Copiar link</button></div>`};
 const card=x=>`<article class="news-card">${media(x)}<div class="news-card-body"><div class="news-card-top"><span class="news-category">${esc(x.categoria)}</span><time datetime="${esc(x.data)}">${fmt(x.data)}</time></div><h3>${esc(x.titulo)}</h3><p>${esc(x.resumo)}</p><div class="news-card-footer"><span class="news-status">${esc(x.status)}</span><a href="noticias.html#${encodeURIComponent(x.id)}">Ler atualização</a></div></div></article>`;
 async function load(){try{const r=await fetch('dados/noticias.json',{cache:'no-store'});if(!r.ok)throw 0;const a=await r.json();a.sort((x,y)=>y.data.localeCompare(x.data));
 const h=document.getElementById('noticias-destaque');if(h)h.innerHTML=a.filter(x=>x.destaque).slice(0,3).map(card).join('')||'<p class="news-loading">Nenhuma atualização publicada.</p>';
 const l=document.getElementById('noticias-lista');if(l)l.innerHTML=a.map(x=>`<article class="timeline-item" id="${esc(x.id)}"><div class="timeline-date"><time datetime="${esc(x.data)}">${fmt(x.data)}</time></div><div class="timeline-card"><div class="news-card-top"><span class="news-category">${esc(x.categoria)}</span><span class="news-status">${esc(x.status)}</span></div><h2>${esc(x.titulo)}</h2><p class="timeline-lead">${esc(x.resumo)}</p>${media(x)}<div class="news-content">${blocks(x)}</div>${share(x)}</div></article>`).join('');
 document.querySelectorAll('.copy-news-link').forEach(b=>b.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(b.dataset.url);const old=b.textContent;b.textContent='Link copiado!';setTimeout(()=>b.textContent=old,1800)}catch(e){prompt('Copie o link:',b.dataset.url)}}));
 }catch(e){document.querySelectorAll('.news-loading').forEach(x=>x.textContent='Não foi possível carregar as atualizações.')}}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load);else load();
})();
