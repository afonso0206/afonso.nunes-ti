document.addEventListener('DOMContentLoaded',async()=>{const root=document.getElementById('spotlight');if(!root)return;let slides=[];try{slides=await(await fetch('dados/slides.json?'+Date.now())).json()}catch(e){slides=[]}if(!Array.isArray(slides)||!slides.length)return;root.classList.add('is-ready');if(slides.length===1)root.classList.add('spotlight-single');const track=root.querySelector('.spotlight-track'),dots=root.querySelector('.spotlight-dots');let current=0,timer=null,paused=false;track.innerHTML=slides.map((s,i)=>`<article class="spotlight-slide${i===0?' is-active':''}" aria-hidden="${i===0?'false':'true'}"><img src="${s.imagem}" alt="${(s.alt||s.titulo||'Imagem de destaque').replace(/"/g,'&quot;')}" width="1600" height="720" loading="${i===0?'eager':'lazy'}"><div class="spotlight-overlay"></div><div class="spotlight-content">${s.categoria?`<span class="spotlight-kicker">${s.categoria}</span>`:''}<h2 class="spotlight-title">${s.titulo||''}</h2>${s.resumo?`<p class="spotlight-text">${s.resumo}</p>`:''}${s.link?`<a class="spotlight-link" href="${s.link}">${s.botao||'Saiba mais'}</a>`:''}</div></article>`).join('');dots.innerHTML=slides.map((_,i)=>`<button class="spotlight-dot${i===0?' is-active':''}" aria-label="Ir para destaque ${i+1}" data-index="${i}"></button>`).join('');const items=[...track.children],dotEls=[...dots.children];function show(i){current=(i+items.length)%items.length;items.forEach((el,n)=>{el.classList.toggle('is-active',n===current);el.setAttribute('aria-hidden',n===current?'false':'true')});dotEls.forEach((el,n)=>el.classList.toggle('is-active',n===current))}function start(){stop();if(slides.length<2||paused||matchMedia('(prefers-reduced-motion: reduce)').matches)return;timer=setInterval(()=>show(current+1),6500)}function stop(){if(timer)clearInterval(timer);timer=null}root.querySelector('.spotlight-prev')?.addEventListener('click',()=>{show(current-1);start()});root.querySelector('.spotlight-next')?.addEventListener('click',()=>{show(current+1);start()});dotEls.forEach(el=>el.addEventListener('click',()=>{show(Number(el.dataset.index));start()}));const pause=root.querySelector('.spotlight-pause');pause?.addEventListener('click',()=>{paused=!paused;pause.textContent=paused?'▶ Continuar':'Ⅱ Pausar';pause.setAttribute('aria-pressed',String(paused));paused?stop():start()});root.addEventListener('mouseenter',stop);root.addEventListener('mouseleave',start);root.addEventListener('focusin',stop);root.addEventListener('focusout',start);start()});
